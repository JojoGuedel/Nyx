// no public modifier
// no abstract modifier
// no extend syntax

// no templates

global func test(mut var a: int) -> int:
    var b: int = 10;
    a += b;
    return a;

global func main():
    mut var a: i32 = 20;
    var b: int = test(10);
    a = test(b);